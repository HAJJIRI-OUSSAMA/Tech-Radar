# Copy to .env and fill in. NEVER commit .env (see .gitignore).
# Not used until later steps — scaffolded now so the shape is fixed.

# Step 1.4 — LLM scoring
ANTHROPIC_API_KEY=

# Step 1.5 — Telegram delivery (@BotFather)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Phase 2 — Supabase (server-side service_role, nightly job only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
